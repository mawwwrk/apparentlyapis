import { useEffect, useState } from "react";

export default function useMarvelAPI(path, query) {
  const [first, setfirst] = useState(second);
  useEffect(() => {
    first;

    return () => {
      second;
    };
  }, [third]);

  return <div>useMarvelAPI</div>;
}
